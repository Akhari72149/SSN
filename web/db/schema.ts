import { boolean, integer, jsonb, pgEnum, pgTable, text, timestamp, uniqueIndex, uuid } from "drizzle-orm/pg-core";
export const eventStatus=pgEnum("event_status",["draft","open","closed","archived"]);
export const attendanceKind=pgEnum("attendance_event_kind",["sign_on","sign_off","correction"]);
export const operationState=pgEnum("sync_operation_state",["pending","accepted","conflict","rejected","expired"]);
export const events=pgTable("events",{id:uuid("id").primaryKey().defaultRandom(),name:text("name").notNull(),timezone:text("timezone").notNull().default("Europe/London"),status:eventStatus("status").notNull().default("draft"),startsAt:timestamp("starts_at",{withTimezone:true}).notNull(),endsAt:timestamp("ends_at",{withTimezone:true}).notNull(),createdAt:timestamp("created_at",{withTimezone:true}).notNull().defaultNow()});
export const staff=pgTable("staff",{id:uuid("id").primaryKey().defaultRandom(),displayName:text("display_name").notNull(),roleLabel:text("role_label"),active:boolean("active").notNull().default(true),rowVersion:integer("row_version").notNull().default(1),createdAt:timestamp("created_at",{withTimezone:true}).notNull().defaultNow(),updatedAt:timestamp("updated_at",{withTimezone:true}).notNull().defaultNow()});
export const shifts=pgTable("shifts",{id:uuid("id").primaryKey().defaultRandom(),eventId:uuid("event_id").notNull().references(()=>events.id),name:text("name").notNull(),startsAt:timestamp("starts_at",{withTimezone:true}).notNull(),endsAt:timestamp("ends_at",{withTimezone:true}).notNull()});
export const shiftAssignments=pgTable("shift_assignments",{id:uuid("id").primaryKey().defaultRandom(),shiftId:uuid("shift_id").notNull().references(()=>shifts.id),staffId:uuid("staff_id").notNull().references(()=>staff.id),cancelledAt:timestamp("cancelled_at",{withTimezone:true}),rowVersion:integer("row_version").notNull().default(1)},t=>[uniqueIndex("shift_assignment_once").on(t.shiftId,t.staffId)]);
export const attendanceEvents=pgTable("attendance_events",{id:uuid("id").primaryKey().defaultRandom(),eventId:uuid("event_id").notNull().references(()=>events.id),assignmentId:uuid("assignment_id").references(()=>shiftAssignments.id),staffId:uuid("staff_id").notNull().references(()=>staff.id),kind:attendanceKind("kind").notNull(),occurredAt:timestamp("occurred_at",{withTimezone:true}).notNull(),recordedAt:timestamp("recorded_at",{withTimezone:true}).notNull().defaultNow(),actorUserId:uuid("actor_user_id").notNull(),correctionOfId:uuid("correction_of_id"),correctionReason:text("correction_reason")});
export const auditEvents=pgTable("audit_events",{id:uuid("id").primaryKey().defaultRandom(),actorUserId:uuid("actor_user_id"),action:text("action").notNull(),targetType:text("target_type").notNull(),targetOpaqueId:uuid("target_opaque_id"),permitted:boolean("permitted").notNull(),requestId:uuid("request_id").notNull(),reason:text("reason"),metadata:jsonb("metadata").notNull().default({}),occurredAt:timestamp("occurred_at",{withTimezone:true}).notNull().defaultNow()});
export const syncOperations=pgTable("sync_operations",{id:uuid("id").primaryKey(),deviceId:uuid("device_id").notNull(),eventId:uuid("event_id").notNull().references(()=>events.id),operationType:text("operation_type").notNull(),payload:jsonb("payload").notNull(),expectedVersion:integer("expected_version"),state:operationState("state").notNull().default("pending"),createdAt:timestamp("created_at",{withTimezone:true}).notNull(),processedAt:timestamp("processed_at",{withTimezone:true})});
